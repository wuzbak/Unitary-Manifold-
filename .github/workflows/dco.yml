name: DCO Check

on:
  pull_request:
    branches: ["main"]

jobs:
  dco:
    name: Developer Certificate of Origin
    runs-on: ubuntu-latest
    steps:
      - name: DCO
        uses: dcoapp/dco@v4
