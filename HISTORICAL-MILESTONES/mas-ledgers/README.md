# MAS Wave Ledgers

Historical MAS wave ledgers and templates are archived here as they are migrated from `docs/mas_tracker.yml`.

For the current machine-readable tracker, see [`../../docs/mas_tracker.yml`](../../docs/mas_tracker.yml).
