[pytest]
markers =
    slow: expensive numerical tests — deselected by default; run with -m slow

addopts = -m "not slow"
python_files = test_*.py ALGEBRA_PROOF.py

# v9.28 note: Unitary Pentad moved to 5-GOVERNANCE/Unitary Pentad/
# Full suite command: python -m pytest tests/ recycling/ "5-GOVERNANCE/Unitary Pentad/" omega/ -q
