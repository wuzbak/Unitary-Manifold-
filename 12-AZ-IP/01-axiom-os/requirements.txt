# AxiomZero Python dependencies
# Core requirements for the AI cognitive layer.
# Optional heavy deps (langgraph, chromadb, playwright) are loaded
# conditionally at runtime with graceful fallback.
# Web framework / API server
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
aiofiles>=23.2
python-multipart>=0.0.9
# Async utilities
anyio>=4.3
# Data / config
pydantic>=2.6
pydantic-settings>=2.2
# Vector store (optional — graceful degradation if absent)
chromadb>=0.4; python_version>="3.10"
# Embeddings (optional)
sentence-transformers>=2.7; python_version>="3.10"
# Agent orchestration (optional — graceful degradation if absent)
langgraph>=0.1; python_version>="3.10"
langchain-community>=0.2; python_version>="3.10"
langchain-ollama>=0.1; python_version>="3.10"
# Symbolic math (optional)
sympy>=1.12
# Browser automation (optional — for M6 web research)
playwright>=1.44; python_version>="3.10"
# Rich terminal output
rich>=13.7
# Testing
pytest>=7.4
pytest-asyncio>=0.23
numpy>=1.24
psutil>=5.9
