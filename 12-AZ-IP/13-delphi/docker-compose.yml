version: "3.9"
services:
  delphi:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    environment:
      - DELPHI_HOST=0.0.0.0
      - DELPHI_PORT=8080
    restart: unless-stopped
